name: Package

on:
  release:
    types: [created]

jobs:
  deploy:
    runs-on: windows-latest
    permissions:
      packages: write
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
      - run: dotnet build --configuration Release SingleFinite.Mvvm.MauiBlazor

      - name: Install .NET MAUI
        run: dotnet workload install maui

      - name: Install WASM tools
        run: dotnet workload install wasm-tools

      - name: Build the project
        run: dotnet build --configuration Release SingleFinite.Mvvm.MauiBlazor

      - name: Create the Blazor package
        run: dotnet pack --configuration Release SingleFinite.Mvvm.Blazor -o packages

      - name: Create the Maui Blazor package
        run: dotnet pack --configuration Release SingleFinite.Mvvm.MauiBlazor -o packages

      - name: NuGet login (OIDC → temp API key)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Publish the package
        run: dotnet nuget push packages\*.nupkg -s https://api.nuget.org/v3/index.json --api-key ${{steps.login.outputs.NUGET_API_KEY}}
